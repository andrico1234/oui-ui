import Checkbox from './checkbox';

export default Checkbox;

// how do I make web components library friendly
// do I need to create a register function whenadding a component?
// is the component itself responsible for loading, or is it the libraries responsibility?

// how does Lion do it, is it something covered by the lit helpers?
// I can do this by creating two exports -
// - a white labelled one
// - one ready to use
